# Seedit Archive server config. Copy to .env (gitignored) on the VPS — see DEPLOY.md.

# The co-located bitsocial-cli daemon's local-only PKC RPC endpoint. The
# container uses host networking, so localhost here really is the VPS host.
# Do not put the daemon's remote auth key in this file; it is unnecessary for a
# loopback connection and could leak through dependency logs.
PKC_RPC_URL=ws://localhost:9138

# Public URL of the web UI (used for sitemap / canonical links).
SITE_URL=https://seeditarchive.org

# Browser origins allowed to call the API, comma-separated (the engine trims
# each entry). seedit.app's in-app search and the canonical seedit.localhost
# dev server fetch this API straight from the browser, so both must stay listed.
# Never leave this unset: the engine falls back to `*` (any origin).
ALLOWED_ORIGINS=https://seeditarchive.org,https://seedit.app,https://seedit.localhost

# Seedit's own SFW verdicts by directory code (URL or path): how the engine
# learns a community is NSFW when its owner never set `features.safeForWork`.
DIRECTORY_DEFAULTS_SOURCE=https://raw.githubusercontent.com/bitsocialnet/lists/master/seedit-directories/seedit-directories-defaults.json

# Operator NSFW overrides — config/nsfw-overrides.json, bind-mounted like the
# blocklist (`[]` when empty) and watched the same way: no restart is required.
NSFW_OVERRIDES_SOURCE=/config/nsfw-overrides.json

# Re-crawl cadence (ms) and pagination depth per pass.
CRAWL_INTERVAL_MS=5000
CRAWL_CONCURRENCY=8
CRAWL_TIMEOUT_MS=30000
CRAWL_MAX_PAGES=20
